# spik.nvim

A fast and simple file picker for Neovim.

## Requirements

- `fd`

## Usage

`:Spik` to open the picker.

## Keymaps

- `<C-n>` / `<C-p>` — navigate
- `<CR>` — open file
- `<Esc>` / `<C-c>` — close
